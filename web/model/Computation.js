'use strict'

module.exports = Computation 

function Computation(algorithm, mst, views = {}) {
    this.algorithm = algorithm
    this.mst = mst
    this.views = views
}

project.prototype.addView = function(computation, ) {

}